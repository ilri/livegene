{% extends '@SonataAdmin/CRUD/list_outer_rows_mosaic.html.twig' %}

{% block sonata_mosaic_background %}
    <img src="{{ object.logoUrl }}" alt="{{ object.headline }}" class="img-responsive center-block" style="width: 100px;">
{% endblock %}

{% block sonata_mosaic_hover_view %}
    <img src="{{ object.logoUrl }}" alt="{{ object.headline }}" class="img-responsive center-block">
{% endblock %}
